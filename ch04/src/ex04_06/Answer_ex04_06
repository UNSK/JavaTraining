練習問題4.5回答

(a)抽象クラス
	具体的な操作が定義されていないため、N分木の構造のみを実装し、必要な操作を抽象メソッドにする。
(b)具象クラス
	(a)のクラスを継承し、探索方法を実装するなど、具体的な操作を定義する必要がある。
(c)インターフェース
	単なるマーカーインターフェースでも良いし、実現したい環境によりは描画に必要なメソッドの設計を書くべき。
(d)具象クラス
	実行するにはインスタンスが必要（？）。
	
練習問題4.6
(a)　必要な操作が定義されていた場合、具象クラスになるが、インターフェース、もしくは抽象クラスに依存した設計にすべき。
(b)　探索アルゴリズムが複数用意する場合は抽象クラス、もしくはインターフェースにすべき。
(c) 描画に必要なメソッドの具体的な実装が決まっている場合は抽象クラス。
(d) 動作環境によって、アプリケーションの起動方法が異なる場合は、抽象クラス。